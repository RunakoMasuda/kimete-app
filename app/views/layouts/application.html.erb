<!DOCTYPE html>
<html>
  <head>
    <title>KimeteApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="http://yui.yahooapis.com/3.18.1/build/cssreset/cssreset-min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <%# ログインしているときは以下を表示するようにしましょう %>
          <% if user_signed_in? %>
            <div class="nav">
              <div class="nav_left">
              </div>
              <div class="nav_right">
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :head_btn %>
                <%# <%= link_to "変更", edit_user_registration_path, class: :head_btn %>  
              </div>
            </div>
          <% else %> 
            <div class="nav">
              <div class="nav_left">
              </div>
              <div class="nav_right">
                <%= link_to "ログイン", user_session_path, class: :head_btn %>
                <%= link_to "新規登録", new_user_registration_path, class: :head_btn %> 
              </div>
            </div>
          <% end %>
      </div> 
    </header>
    <%= yield %>
    <footer class = "footer">
      <p class="copyright">
        Copyright kimeteapp 2021.
      </p>
    </footer>
  </body>
</html>
